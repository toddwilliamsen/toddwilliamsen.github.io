# ARM samples moved

The downloadable ARM templates now live in a dedicated repository:

**[github.com/toddwilliamsen/arm-templates](https://github.com/toddwilliamsen/arm-templates)**

Clone and deploy from there — see that repo’s README for paths and `az deployment` examples.
