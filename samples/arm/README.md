# ARM & Bicep samples moved

Downloadable ARM JSON and Bicep samples live in a dedicated repository:

**[github.com/toddwilliamsen/arm-templates](https://github.com/toddwilliamsen/arm-templates)**

Clone and deploy from there — see that repo’s README for ARM paths, Bicep samples (`bicep/storage`, `bicep/vnet`, `bicep/keyvault`), and `az deployment` examples.
